# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# Get your MongoDB connection string from MongoDB Atlas
# Make sure to URL-encode special characters in the password
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/DatabaseName?retryWrites=true&w=majority

# Google AI Configuration
# Get your API key from https://ai.google.dev/
GOOGLE_API_KEY=your_google_api_key_here

# Email Configuration (for OTP emails)
# Use Gmail SMTP or your preferred email provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password

# Pinecone Configuration
# Get your API key and create an index at https://www.pinecone.io/
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_INDEX=auto-triager
PINECONE_NAMESPACE=support-docs

# Webhook Configuration (optional)
# Set this if you want to receive webhook notifications for ticket events
WEBHOOK_URL=https://example.com/webhook
